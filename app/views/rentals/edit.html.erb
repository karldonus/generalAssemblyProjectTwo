<h2>Edit Rental</h2>

<%= render partial: "form" %>

<%= form_for(@rental, method: :delete, data: { confirm: 'Are you sure?' }) do |f| %>
  <%= f.submit "DELETE", class: 'btn btn-danger btn-md' %>
<% end %>
