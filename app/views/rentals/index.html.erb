<h2>Available Rentals <%= link_to image_tag("plus-circle.png", size: "20"), new_rental_path%></h2>

<ul>
  <% @rentals.each do |rental| %>
    <li>
      <%= link_to(rental.property_name, rental_path(rental))%>
      </a>
    </li>
  <% end %>
</ul>
